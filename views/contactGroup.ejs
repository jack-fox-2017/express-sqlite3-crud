<!DOCTYPE html>
<html>
  <head>
  <style>
    ul {
      list-style-type: none;
      margin: 0;
      padding: 0;
      overflow: hidden;
      background-color: #333;
    }

    li {
      float: left;
    }

    li a {
      display: block;
      color: white;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
    }

    li a:hover {
      background-color: #111;
    }
    </style>
    <meta charset="utf-8">
    <title>Groups</title>
  </head>
  <body>
    <ul>
      <li><a class="active" href="/">Home</a></li>
      <li><a href="contacts">Contact</a></li>
      <li><a href="groups">Group</a></li>
      <li><a href="addresses">Address</a></li>
      <li><a href="profiles">Profile</a></li>
    </ul>
    Contact Name: <br>
    <%= data_contact[0].name %>

    <form action="/contacts/group/<%= data_contact[0].id%>" method="post">
        <select name="groups_id">
        <% data_group.forEach( cont => {%>
          <option value="<%= cont.id%>"><%= cont.name_of_group%></option>
        <%})%>
        </select>
      <button class = 'button_3' type="submit">Submit</button>
    </form>

  </body>
</html>
